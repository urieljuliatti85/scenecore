Hi <%= @user.name %>,

A SceneCore administrator reviewed your request to administer <%= @band.name %>
and did not approve it this time. You can submit a new request from the
band's page whenever you'd like.

<%= public_band_url(@band.slug) %>
