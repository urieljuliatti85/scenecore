<section class="px-4 sm:px-6 py-12 sm:py-16 max-w-xl mx-auto">
  <h1 class="text-3xl font-extrabold tracking-tight mb-2">New core session</h1>
  <p class="text-neutral-500 mb-8"><%= @band.name %></p>

  <%= render "form", band: @band, core_session: @core_session, submit_label: "Create session" %>
</section>
