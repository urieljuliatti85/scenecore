<section class="px-4 sm:px-6 py-12 sm:py-16 max-w-xl mx-auto">
  <h1 class="text-3xl font-extrabold tracking-tight mb-2">Edit post</h1>
  <p class="text-neutral-500 mb-8"><%= @band.name %></p>

  <%= render "form", band: @band, post: @post, submit_label: "Save changes" %>
</section>
