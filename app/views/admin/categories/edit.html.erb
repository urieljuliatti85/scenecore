<% content_for :title, "Edit category — Admin — SceneCore" %>

<div class="max-w-xl">
  <h1 class="text-3xl font-extrabold tracking-tight mb-8">Edit category</h1>

  <%= render "form", category: @category, submit_label: "Save changes" %>
</div>
