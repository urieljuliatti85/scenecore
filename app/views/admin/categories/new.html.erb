<% content_for :title, "New category — Admin — SceneCore" %>

<div class="max-w-xl">
  <h1 class="text-3xl font-extrabold tracking-tight mb-8">New category</h1>

  <%= render "form", category: @category, submit_label: "Create category" %>
</div>
